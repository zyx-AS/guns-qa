# CI rerun marker for GUNSQA-79

Purpose: trigger the managed CI evidence flow after the Jira Test description was updated with database-participation coverage on 2026-05-04.

Expected automation:

- Resolve `testClass` from `config/xray-test-executions.json`.
- Reuse Xray Test Execution `GUNSQA-89`.
- Reuse mapped Bug `GUNSQA-94` when the run is classified as `test-assertion-failed`.
- Write the Bug key back to the Jira evidence comment.
